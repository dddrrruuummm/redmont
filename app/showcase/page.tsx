import type { Metadata } from "next"; import { ProductCard } from "@/components/product-card"; import { products } from "@/lib/site";
export const metadata: Metadata = { title: "Focus Areas", description: "REDMONT representative asset management focus areas." };
export default function ShowcasePage(){return <section className="bg-white py-16"><div className="container-page"><p className="eyebrow text-wine">Focus Areas</p><h1 className="mt-4 text-5xl font-bold">The practical work around capital assets.</h1><p className="mt-5 max-w-2xl text-ink/70">Representative areas for commercial collaboration, not a claim of regulated investment advisory services. Confirmed offerings can be added as the business develops.</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard key={p.name} product={p}/>)}</div></div></section>;}
