import type { Metadata } from "next"; import { ProductCard } from "@/components/product-card"; import { products } from "@/lib/site";
export const metadata: Metadata = { title: "Spaces", description: "Hospitality, commercial, residential, procurement, and construction work by REDMONT." };
export default function ShowcasePage(){return <section className="bg-white py-16"><div className="container-page"><p className="eyebrow text-wine">Spaces</p><h1 className="mt-4 text-5xl font-bold">From a first layout to the final finish.</h1><p className="mt-5 max-w-2xl text-ink/70">REDMONT supports hospitality, commercial, and residential environments with an integrated approach to design, procurement, and construction delivery.</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{products.map(p=><ProductCard key={p.name} product={p}/>)}</div></div></section>;}
