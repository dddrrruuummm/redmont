import type { Metadata } from "next"; import { services } from "@/lib/site"; import { Check } from "lucide-react";
export const metadata: Metadata = { title: "Approach", description: "REDMONT capital asset management operations and planning approach." };
export default function ServicesPage(){return <section className="bg-white py-16"><div className="container-page"><p className="eyebrow text-wine">Approach</p><h1 className="mt-4 text-5xl font-bold">Disciplined asset work, clearly structured.</h1><div className="mt-10 grid gap-5 md:grid-cols-3">{services.map(s=><div key={s} className="bg-pearl p-6"><Check className="text-wine"/><h2 className="mt-4 text-xl font-bold">{s}</h2><p className="mt-3 text-sm text-ink/65">A practical starting point for a focused business conversation.</p></div>)}</div></div></section>;}
